allow vold efs_file:dir rw_file_perms;

allow vold sysfs_block_writable:file rw_file_perms;

# Based on the comment from b/111409607 FITRIM might
# be needed
allow vold mnt_vendor_file:dir { open read ioctl };
